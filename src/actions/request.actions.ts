// // @todo: Retrieve the public key of the user from the database and encrypt the request before sending it to the server.

// export const encryptPayload = (payload: any) => {
//     return CryptoJS.AES.encrypt(JSON.stringify(payload), process.env.REACT_APP_CRYPTO_KEY).toString();
// }

export default null;